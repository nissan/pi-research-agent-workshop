# Harness Failure Demo Notes

The harness blocks non-arXiv network access and risky shell actions during the workshop. This makes the agent safer and creates a traceable policy boundary that a downstream buyer or agent can inspect.
